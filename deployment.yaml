apiVersion: apps/v1
kind: Deployment
metadata:
  name:  springboot
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app:  springboot
  template:
    metadata:
      labels:
        app:  springboot
    spec:
      containers:
        - name:  springboot
          image: kunlekush/spring1:app-a7c78313b72cfbcf11acab9137b785418669b884
          ports:
            - containerPort: 80 
          imagePullPolicy: Always
      #imagePullSecrets: 
      #  - name: docker-cred 
---
apiVersion: v1
kind: Service
metadata:
  name:  springboot
  namespace: default
spec:
  selector:
    app: springboot
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
